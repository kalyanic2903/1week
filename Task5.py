#--------------To calculate the area of a tringle given its base & height using the formula-------

# Input: Base & Height
base = float(input("Enter the base of the triangle: "))
height = float(input("Enter the height of the triangle: "))

# Calculate the area 
area = (base * height)/2

# Output the result
print("The area of the triangle is :",area)
